apiVersion: v1
kind: Service
metadata:
  labels:
    app: cart-cs
  name: cart-cs
spec:
  selector:
    app: cart
  ports:
  - port: 80
    targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: cart-ext
  name: cart-ext
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: cart
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: cart
  name: cart
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart
  template:
    metadata:
      labels:
        app: cart
    spec:
      containers:
        - name: cart
          imagePullPolicy: Never
          env:
            - name: MONGO_URL
              value: mongo-hs
          image: cart
          ports:
            - containerPort: 8080
      restartPolicy: Always
